// run simulation button id = 'run-simulation-btn' (from compiler.html)
const runSimulationButton = document.getElementById('run-simulation-btn');

if (runSimulationButton) {
    runSimulationButton.addEventListener('click', () => {
        console.log('simulation button clicked');  // msg printed when run simulation btn is clicked (for debugging)
        // add func. here
    });
}